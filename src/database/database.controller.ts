import { Controller, Post, Body, Inject, Get, Param, DefaultValuePipe, ParseIntPipe } from '@nestjs/common';
import { DatabaseService } from './database.service';

@Controller('database')
export class DatabaseController {
    
}
